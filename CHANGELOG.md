# 更新日志

## v0.2.0 (2026-09-03)

按官方开发文档（1.2.3 基线）全面合规修订：

- **规则级独立时区**：每条规则可设置 `timezone_offset`（支持半小时区如 `5.5`），适配不同厂商错峰时段按不同时区公布的场景；是否生效由开关 `use_global_timezone` 控制（默认跟随全局）；
- **修复 WebUI 配置保存失败**：`Unable to convert NoneType to a TOML item`。规则时区字段原以 `null`（留空）表达"跟随全局"，但 TOML 不支持 null，导致 WebUI 中开关/保存插件配置时写回 TOML 崩溃；改为显式布尔开关 + 始终有值的数值字段，杜绝 null；
- **重试轮保护**：指定模型失败后的重试轮不再指名，交还麦麦策略兜底，避免反复请求刚失败的模型直至重试耗尽；
- **兼容性声明**：最低麦麦版本保持 `1.2.0`（1.2.0 主机实测钩子/热更新/校验全部可用，与 doubao-tts 声明一致）、SDK 下限 `2.5.0`；manifest 补充 `changelog` 字段；
- **澄清生效任务范围**：`task_names` 仅 `replyer` 有效（官方钩子不支持为 planner 等其他任务指定模型），配置描述更正并在加载时对无效任务名给出警告；
- 日志统一走 `ctx.logger`（自动 IPC 转发进主日志）；时区显示支持小数（如 UTC+5.5）。

## v0.1.0 (2026-09-03)

首个版本。

### 核心功能

- 多条"时段 → 模型"调度规则：按一天中的不同时间段指定不同模型，多条命中时取第一条；
- 跨午夜区间支持：如 `22:00 ~ 06:00` 可直接书写，无需拆分为两条规则；
- 兜底模型：`default_model_name` 可在无规则命中时指定兜底模型，留空则不干预；
- 时区偏移：`timezone_offset` 自定义时区（默认北京时间 `+8`）；
- 热更新：修改 `config.toml` 即时生效，无需重启麦麦；
- 零干预安全设计：无规则命中时不干预麦麦原有策略；插件出错只记日志不阻断回复（`ErrorPolicy.SKIP`）。
